import React, { FC, ReactElement } from 'react';

import { CompartmentProps } from '../../../api/api.types';

const StatusForm: FC<CompartmentProps> = ({
	contentValue,
	value,
	onChange,
}): ReactElement | null => {
	/**
	 * METHODS
	 */

	/**
	 * RENDER
	 */
	return <div>Status form</div>;
};

export default StatusForm;
